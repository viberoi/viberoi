"""Shared pytest fixtures for viberoi-shared tests.

Will grow to include: DB session fixture (test schema + RLS),
Redis fixture, LocalStack fixtures (S3, SQS, KMS), fake Cognito
JWT issuer, deterministic crypto fixtures.
"""
