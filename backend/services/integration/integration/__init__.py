"""VibeROI Integration service.

OAuth flows, external API calls to GitHub / Jira / Linear, webhook
registration on the provider side, ticket + sprint backfill / delta sync.
See CLAUDE.md.
"""

__version__ = "0.1.0"
